#!/usr/bin/env bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
HERE="$( cd "$DIR/.." && pwd)"
PREF_DIR=$HOME/Library
OUT_NAME=$HERE/prefs

cd $HERE

echo "Copying \"$OUT_NAME/*\" to \"$PREF_DIR/\""
cp -R $OUT_NAME/* $PREF_DIR/

echo "*** DONE"
