#!/usr/bin/env bash

APPLICATION_NAME=$1

>&2 echo "----> Install '${APPLICATION_NAME}'..."
brew cask list | grep $APPLICATION_NAME
if [[ $? -ne 0 ]]; then
  brew cask install $APPLICATION_NAME
fi

>&2 echo "----> Done installing application."
exit 0
